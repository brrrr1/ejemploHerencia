package com.salesianos.herencia.mappedSuperClass.repos;

import com.salesianos.herencia.mappedSuperClass.model.PlantillaFutbol;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlantillaFutbolRepository extends JpaRepository<PlantillaFutbol, Long> {
}
